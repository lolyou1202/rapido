import { generateIdEdition } from './generateIdEdition'

export const generateInitialEditionList = ({
	countGenerate,
}: {
	countGenerate: number
}) => {
	const idListEdition = generateIdEdition({ editionsList: [], countGenerate })

	
}
